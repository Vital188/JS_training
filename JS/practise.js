function Hero (name) {
let name = {
    name: 'Hero',
    position: '00',
    health: 100,
    damage: 5,
    experience: 0
  };
  return `Hero should have a ${Hero.key} attribute with value ${Hero.val}`
}

console.log((name.position))